Select class method



//Java code

Select sc = new Select(driver.findElement(By.name("cars")));

//how to select your dropdown
Method 1
sc.selectByVisibleText("Audi")


Method 2
sc.selectByIndex("3");

Method 3
sc.selectByValue("audi")


Action : type something and press enter

driver.findElement(By.name("q")).sendkeys("Mobile", Keys.TAB, Keys.ENTER)

//pressing multiple keys at once

driver.findElement(By.name("q")).sendkeys("Mobile", Keys.chord( Keys.TAB, Keys.ENTER, "C"))


GetAttribute / getcssvalue /getText()


Ex  <input id ="email" type="email" autofocus='' placeholder = ''   >

Coding attribute contains attributes like id, name, type etc

Styling attribute can be thought of as attributes which ae used for formatting the element like border width, size etc



.getAttribute("Style")           when it is directly an attribute
We use this when we want value of any attribute inside a tag


.getcssValue()      gets the style which has been applied to that tag.    



Table 
tr
td
img


FindElements

Normaly index starts from 0

Q ::  Click on 3rd Image on 4th Column in 3rd row in 2nd Table

List <WebELement> l1 = driver.findElements(By.tagname("table"))

List <WebELement> l2 = l1.get(1).driver.findElements(By.tagname("tr"))

List <WebELement> l3 = l2.get(2).driver.findElements(By.tagName("td"))

List <WebELement> l4 = l3.get(3).driver.findElements(By.tagName("img"))

l4.get(2).click();


Q    :  

Select India from 3rd drodown out of 
3 different Dropdown in 
4th COlumn in 
4th Row in 
2nd Table

List <WebELement> l4 = l3.get(3).driver.findElements(By.tagName("Select"))
all the dropdowns


Select sc = new Select(l4.get(2));

For Last Element we can use
Select sc = new Select(l4.get(l4.size() -1));

sc.SelectByVisibleText("India")


// Xpath


















